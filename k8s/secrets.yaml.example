# Template only — install-shop-demo.sh generates the real Secret at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: shop-demo-secrets
  namespace: shop-demo
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  JWT_SECRET: "change-me"
